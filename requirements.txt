Flask==3.0.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.31
Werkzeug==3.0.3
python-dotenv==1.0.1
gunicorn==22.0.0
psycopg2-binary==2.9.9
# Analytics / export
pandas==2.2.2
openpyxl==3.1.5
reportlab==4.2.2
# PDF: ReportLab (above) is the default engine and always works.
# WeasyPrint produces nicer HTML-based PDFs but needs native GTK libraries
# (awkward on Windows; on Render add an apt build step). It is OPTIONAL — the
# export service auto-detects it and falls back to ReportLab when absent.
# To enable, uncomment:
# WeasyPrint==62.3
